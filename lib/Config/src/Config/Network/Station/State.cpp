#include "./State.hpp"

namespace Config {
  namespace Network {
    namespace Station {

      State::State(const JsonObject & object) {
      }

      void State::serialize(JsonObject & object) const {
      }

    }
  }
}
