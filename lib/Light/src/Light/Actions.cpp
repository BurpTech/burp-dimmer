#include "Actions.hpp"

namespace Light {
  namespace Actions {

    const Action toggle(ActionType::TOGGLE);
    const Action turnOn(ActionType::TURN_ON);
    const Action turnOff(ActionType::TURN_OFF);
    const Action increaseBrightness(ActionType::INCREASE_BRIGHTNESS);
    const Action decreaseBrightness(ActionType::DECREASE_BRIGHTNESS);
    const Action applyConfig(ActionType::APPLY_CONFIG);

  }
}
